interface Record {
    videoId: string, 
    currentTime: number, 
    duration: number, 
    seriesId: string, 
    videoName: string,
    categoryId: string,
    viewEpoch: number
}
export default Record