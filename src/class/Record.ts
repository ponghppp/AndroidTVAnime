interface Record {
    videoId: string, 
    currentTime: number, 
    duration: number, 
    seriesId: string, 
    videoName: string,
    seriesName: string,
    viewEpoch: number,
    source: string
}
export default Record